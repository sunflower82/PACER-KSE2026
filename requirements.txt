# =============================================================================
# DAMPS-MMHCL -- pip dependencies
# =============================================================================
# Required deps cover the locked Revision 9 architecture; OPTIONAL deps unlock
# the additional accelerations from the Training Speedup Guide (sections shown
# inline below).

# ----- Core deep-learning stack -----
torch>=2.1.0
numpy>=1.21.0
scipy>=1.9.0           # paired t-test (scipy.stats.ttest_rel) -- speedup S8
scikit-learn>=1.0.0

# ----- Progress / utility -----
tqdm>=4.65.0

# ----- OPTIONAL speedup add-ons (uncomment as needed) -----
# Bayesian HPO with Optuna -- TPESampler + HyperbandPruner (Speedup S6).
# optuna>=3.2.0

# Weights & Biases experiment tracking + Bayesian sweeps (Speedup S7).
# wandb>=0.15.0
# nbformat>=5.7.0      # Required by wandb to save notebook history in Jupyter/Colab.
# pyyaml>=6.0          # Required by run_wandb_sweep.py to parse the YAML.

# FAISS GPU mandatory fallback for K-NN when N >= 60_000 (Speedup S2).
# Linux: pip install faiss-gpu>=1.7.4
# CPU-only systems can substitute: pip install faiss-cpu>=1.7.4
# faiss-gpu>=1.7.4

# Optional readability sugar for tensor reshapes (Speedup S10).
# einops>=0.7.0

